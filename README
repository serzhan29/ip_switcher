# IP Switcher

Простой инструмент для переключения IP-адресов в Windows.

## Установка

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/serzhan29/ip_switcher.git
   cd ip_switcher

2. Создайте и активируйте виртуальное окружение:
  - python -m venv .venv
  - .venv\Scripts\activate

3. Установите зависимости:
pip install -r requirements.txt


Структура проекта
ip_switcher/
│── .venv/            # Виртуальное окружение
│── ip_switcher/      # Исходный код проекта
│── requirements.txt  # Зависимости
│── README.md         # Документация


Чтобы превратить твой Python-проект в exe файл, тебе нужен инструмент вроде PyInstaller.
🔹 1. Установи PyInstaller
pip install pyinstaller
🔹 2. Перейди в папку с проектом
cd D:\PycharmProjects\PythonProject\ip_switcher

🔹 3. Собери exe
  Если у тебя есть основной файл, например main.py, запускай:
    pyinstaller --onefile main.py

  --onefile — соберёт в один .exe файл (иначе будет куча файлов).
  
  --noconsole — если хочешь скрыть консольное окно (для GUI-приложений).

Пример:
  pyinstaller --onefile --noconsole main.py

🔹 4. Где искать exe
  После сборки появятся папки:
    build/ — временные файлы
    dist/ — там будет твой main.exe

🔹 5. Запуск
Проверь так:
  dist\main.exe
